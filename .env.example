# ===========================================
# Copia este archivo como .env y rellena los valores.
# Nunca subas .env al repositorio.
# ===========================================

# ----- Servidor (server.cjs) -----
PORT=4000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_contraseña_mysql
DB_NAME=chatBot

# Orígenes permitidos por CORS (separados por coma)
CORS_ORIGINS=http://localhost:1420,http://localhost:3001

# API DeepSeek (para respuestas del ChatBot). Obtén la clave en https://platform.deepseek.com
DEEPSEEK_API_KEY=

# Opcional: API OpenAI (alternativa al ChatBot). https://platform.openai.com/api-keys
# OPENAI_API_KEY=

# ----- Frontend (Vite / React) -----
# URL del API/servidor (usada por Socket.io y fetch)
VITE_API_URL=http://localhost:4000

# Opcional: URL del endpoint PHP para subir imágenes en tickets (si usas upload.php)
# VITE_UPLOAD_PHP_URL=http://localhost/upload.php
